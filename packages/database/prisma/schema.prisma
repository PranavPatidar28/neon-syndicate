generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Intentionally model-free. Design User and Session in Identity Protocol, not before.
