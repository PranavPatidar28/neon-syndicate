# Current Handoff

Foundation and the local Mission Control engine are ready. No product quest is implemented. Mission Control includes a deterministic next-move compass and an optional structured mentor backed by the local Antigravity Claude Proxy. Start the proxy, run `pnpm mission-control`, open `http://127.0.0.1:3100`, and start `01-01` when ready. Future agents must follow `AGENTS.md`, preserve the separation between Mission Control and learner product apps, and inspect actual test evidence before helping.
